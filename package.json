{
  "type": "module",
  "name": "server",
  "version": "1.0.0",
  "description": "server for chatgpt prg08 cmgt",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --env-file=.env server.js",
    "dev": "nodemon --env-file=.env --watch server.js",
    "vercel-build": "echo hello"
  },
  "author": "LNH",
  "license": "ISC",
  "dependencies": {
    "@langchain/openai": "^0.0.14",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "langchain": "^0.1.21",
    "openai": "^4.28.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
